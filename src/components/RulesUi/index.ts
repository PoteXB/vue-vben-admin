import { withInstall } from '/@/utils';
import required from './src/Required.vue';
import explain from './src/Explain.vue';

export const Required = withInstall(required);
export const Explain = withInstall(explain);
